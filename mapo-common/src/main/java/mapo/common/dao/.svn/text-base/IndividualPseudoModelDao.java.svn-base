package mapo.common.dao;

import java.util.List;

import mapo.common.PSeudoModel;

public interface IndividualPseudoModelDao {
	
	/**
	 * Writes a pseudo model to the cache
	 * @param pseudoModel
	 * @author jmayaalv
	 */
	public void write(long individual, PSeudoModel pseudoModel);

	/**
	 * Obtains the pseudo models from an individual
	 * @param individual
	 * @return
	 * @author jmayaalv
	 */
	public List<PSeudoModel> get(long individual);

}
